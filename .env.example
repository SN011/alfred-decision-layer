# Set ONE of these LLM API keys (Groq recommended — fast + free tier)
GROQ_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=


# Optional: override the model name
ALFRED_MODEL=grok-4-1-fast-reasoning

# Server port (default 8080)
# PORT=8080
